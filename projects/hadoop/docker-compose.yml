version: "3.7"

services:
  master:
    build:
      context: .
      dockerfile: ./master/Dockerfile
    ports:
      - "9870:9870"
      - "8088:8088"
      - "19888:19888"
    container_name: master
    volumes: 
      - ./data:/data_volume
  worker:
    build:
      context: .
      dockerfile: ./worker/Dockerfile
